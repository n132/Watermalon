
//Problem2066


#include<stdio.h>
int main()
{
	char a[] = "javaman";
	char b[] = "cpcs";
	char c[] = "same";
	int n;
	scanf("%d", &n);
	for (int i = 0; i < n; i++)
	{
		int y1, y2, m1, m2, d1, d2;
		scanf("%d%d%d%d%d%d", &y1, &m1, &d1, &y2, &m2, &d2);
		if (y1 < y2)
		{
			printf("%s\n", a);
		}
		else if (y1 > y2)
		{
			printf("%s\n", b);
		}
		else
		{
			if (m1 < m2)
			{
				printf("%s\n", a);
			}
			else if (m1 > m2)
			{
				printf("%s\n", b);
			}
			else
			{

				if (d1 < d2)
				{
					printf("%s\n", a);
				}
				else if (d1 > d2)
				{
					printf("%s\n", b);
				}
				else
				{
					printf("%s\n", c);
				}
			}
		}
	}
}



