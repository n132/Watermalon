
//Problem2130


#include<math.h>
#include<stdio.h>
int main()
{
    double a, b, c;
    while (scanf("%lf%lf%lf",&a,&b,&c)!=EOF)
    {
        double t = b*b - a * 4 * c;
        if (t < 0) printf("NO\n");
        else
        {
            printf("%.2lf %.2lf\n", (-b + sqrt(t) )/ (2 * a), (-b - sqrt(t)) / (2 * a));
        }
    }
}



