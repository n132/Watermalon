
	//Problem2503


	#include<stdio.h>
int main()
{
	int t;
	int i;
	int n, m;
	int q, w, e, r;
	scanf("%d/%d / %d/%d", &q, &w, &e, &r);
	n = q*r;
	m = w*e;
	for (i = 1; i <= m; i++)
	{
		if (m%i == 0 && n%i == 0)
		{
			t = i;

		}
	}
	printf("%d/%d", n / t, m / t);
}



